name: ci
on:
  push:
    branches: ["**"]
jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - name: Dagger
        uses: dagger/dagger-for-github@v6
        with:
          module: github.com/goreleaser/goreleaser@v2.3.2
          args: test output
